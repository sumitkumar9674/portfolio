// ------------------------------------------------------------
// HomeScreen
// ------------------------------------------------------------
// Content displayed on the HOME face of the 3D cube.
//
// This component is intentionally independent from the cube.
// It can later contain the complete Home portfolio experience,
// including text, images, animations, and attached 3D objects.
// ------------------------------------------------------------
import "./HomeScreen.css";

export default function HomeScreen() {
  return (
    <div className="homeScreen">
      {/* Temporary HOME content while the screen architecture is built. */}
      HOME
    </div>
  );
}
